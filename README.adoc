= Tic Tac Toe Game

=== How to run locally?

. Run compiler: `sbt compile`

. Run tests: `sbt test`

. Run project: `sbt run` 

=== Bugs

* Draw is not rendering

* Cannot rerender elements after Gamer wants to continue
